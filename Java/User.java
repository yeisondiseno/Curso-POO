class User extends Account {

    public User( 
        Integer id, 
        String name, 
        String email, 
        String document, 
        String password) {
        super( id, name, email, document, password );
    }
}
