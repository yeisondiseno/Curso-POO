class Driver extends Account {

    public Driver( 
        Integer id, 
        String name, 
        String email, 
        String document, 
        String password) {
        super( id, name, email, document, password );
    }
}
